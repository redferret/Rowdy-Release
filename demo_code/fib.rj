import "rowdy.core"

func main(max) {
	time = $runtime(func() {
		$printFibs(is max? 10)
	})
	
	print "Took ", time, " ms to finish", ln
}

func printFibs(max) {
	x = 0 as long
	z = 0 as long
	y = 1 as long
	$forLoop(0, max, 1, func(){
		print x, ln
		z = x + y
		x = y
		y = z
	})
}
